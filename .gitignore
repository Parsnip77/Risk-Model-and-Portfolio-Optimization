# Local-only docs (never push to remote; project docs for remote will be written later)
prompt.md
Instruction.md

.cursor/

# Tushare token is stored here; must never be committed
src/config.py

# Generated database file (regenerate with download_data(); too large for version control)
data/*.db
data/*.parquet

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# macOS
.DS_Store
